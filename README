--RUN--
Install the dependencies with: npm install -d
Run the application with: sudo node app.js (sudo is needed to run it on port 80)

--TODO--
- Save for real the file
- Retrive the file
- Does it need to be persistent? (for text area)